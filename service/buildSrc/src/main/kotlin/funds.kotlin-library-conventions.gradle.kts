// TODO(Johann) research a bit more how could you have a build applied to multiple submodules. and allow this build to access toml catalogs (unlike buildSrc)
plugins {
    id("funds.kotlin-common-conventions")
    `java-library`
}
