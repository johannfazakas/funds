# Tasks — analytics-dashboards

## 1. API models

- [x] 1.1 Dashboard TOs in analytics-api: `DashboardTO`, `DashboardChartTO`, `DashboardLookbackTO` (amount + `DashboardLookbackUnitTO` enum), plus request TOs following the Create/Update convention (`CreateDashboardTO`, `UpdateDashboardTO`, `CreateDashboardChartTO`); init-block validation (non-blank names, positive lookback amount, non-empty chart queries, unique non-blank query ids per chart)
- [x] 1.2 Settings fields renamed with the `default` prefix end to end (`defaultGranularity`, `defaultLookback`, `defaultTargetCurrency`; `default_*` DB columns) and domain write models renamed to `CreateDashboard`/`UpdateDashboard`/`CreateDashboardChart`

## 2. Service: persistence and API

- [x] 2.1 Flyway migration `V3__add_dashboard_tables.sql`: `dashboard` and `dashboard_chart` tables (positions, settings columns, jsonb queries, cascade delete)
- [x] 2.2 `DashboardRepository` (Exposed): list/get by user, create, transactional full replace, delete, append chart with next position; queries column serialized via kotlinx JSON with `ignoreUnknownKeys`
- [x] 2.3 `DashboardService`: user scoping (foreign/unknown id → not found), position assignment for new dashboards and appended charts
- [x] 2.4 `web/DashboardApiRouting.kt`: the six endpoints mapping TOs ↔ domain; 404 via the existing error handling; wire routing and Koin singles in `AnalyticsDependencies`/`AnalyticsRouting`

- [x] 2.5a Chart-level API: `DashboardQueryTO` (mandatory `label`), `UpdateDashboardChartTO`, `UpdateDashboardChartPositionsTO`; `PUT`/`DELETE /charts/{chartId}` and `PUT /charts/positions`; dashboard `PUT` reduced to metadata (name + defaults); `DashboardChartNotFoundException` → 404
- [x] 2.5 Dashboard reordering: `UpdateDashboardPositionsTO` (non-empty, unique ids), `PUT /dashboards/positions` renumbering positions 0..n-1 transactionally; request must reference each of the user's dashboards exactly once (missing/unknown/duplicate → 400 via `DashboardReorderException`)

## 3. Tests

- [x] 3.1 Dashboard API test: create/get/list round-trip (position order, settings, lookback echoed), full-update reorder + chart edit preserving chart ids, delete removes charts, chart append gets last position without touching others
- [x] 3.2 Validation and isolation tests: blank names / non-positive lookback / empty queries / duplicate query ids → 400; GET, PUT, DELETE, and append against another user's dashboard → 404 and unchanged data

- [x] 3.2a Chart lifecycle tests: chart update keeps id/position, isolated chart delete, chart reorder + completeness 400, blank-label 400, foreign/unknown chart 404s
- [x] 3.3 Reorder tests: three dashboards reordered → new order with compacted positions; missing/unknown/duplicate/foreign ids → 400 with order unchanged

## 4. Web client

- [x] 4.1 `dashboardApi.ts`: typed client for the six endpoints
- [x] 4.2 Extract shared chart rendering from `AnalyticsPage` into `chartAssembly.ts` (incremental report builder from stream events + line/label/color derivation) and re-use it in the analytics page
- [x] 4.3 Dashboard view page (`/dashboards/:id`): resolve lookback to an absolute interval at open, one `streamMetricsReport` per chart rendering into `MultiSeriesChart`; per-chart spinner and error state
- [x] 4.4 Dashboards list page (`/dashboards`): list, create, delete, open; sidebar section listing dashboards by name linking to their pages
- [x] 4.5 Dashboard management page (`/dashboards/:id/edit`): name + settings controls, chart list with add/remove/position up/down, per-chart name + query editors (reusing `MetricQueryEditor`), Save → PUT, Cancel discards
- [x] 4.4a Dashboards list reordering: up/down arrows per row calling `reorderDashboards` and refreshing the sidebar
- [x] 4.6a Dashboard view settings bar: granularity, from/to date pickers (derived from the default lookback), and currency controls initialized from stored defaults; switching re-streams all charts (view-time only, not persisted)
- [x] 4.6b Query labels in the UI: mandatory per-query labels (autogenerated until edited) replacing the A/B/C notation in legend and editors; the Y axis is always zero-based (extends below zero for negatives)
- [x] 4.6c Chart edit page (`/dashboards/:id/charts/:chartId/edit`) with name/query editing and live preview; dashboard view chart cards gain an edit pencil; edit-dashboard page reduced to metadata + chart list (immediate reorder/delete, add-chart opens the new page)
- [x] 4.6 Analytics page "Add to dashboard": dialog with dashboard select + chart name, POST append of the current query list, noting the chart adopts the dashboard's period and currency

## 5. Docs and verification

- [x] 5.1 Full analytics suite green; rebuild and redeploy `ff_analytics` and the web client; smoke-test end to end (create dashboard, append from analytics page, reorder, view page streams all charts)
